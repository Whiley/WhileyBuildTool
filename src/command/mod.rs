mod build;
mod clean;
mod init;
mod install;
mod run;

pub use build::build;
pub use clean::clean;
pub use init::init;
pub use install::install;
pub use run::run;

// Help command

// Init command

// Install command
